{
    "name": "localpdf-extension",
    "private": true,
    "version": "2.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "package": "npm run build && zip -r localpdf-extension-v2.0.0.zip dist",
        "preview": "vite preview"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.287",
        "typescript": "^5.7.2",
        "vite": "^6.0.5",
        "vite-plugin-static-copy": "^2.2.0"
    }
}